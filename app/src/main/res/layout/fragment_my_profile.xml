<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.mainActivity.MyProfileFragment">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/imgCircleViewProfilePhoto"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/profile_photo"
        app:civ_border_width="2dp"
        android:layout_centerHorizontal="true"
        android:layout_margin="30dp"/>

    <TextView
        android:id="@+id/txtUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Username Here"
        android:layout_below="@+id/imgCircleViewProfilePhoto"
        android:textSize="25sp"
        android:textColor="#000000"/>

    <TextView
        android:id="@+id/txtUserBio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="I will never give up"
        android:layout_below="@+id/txtUsername"
        android:layout_margin="10dp"
        android:textSize="20sp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerviewMyPosts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/txtUserBio"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="5dp"/>

    <!--<Button
        android:id="@+id/btnLogOut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Log Out"
        android:background="@drawable/button_style"
        android:layout_margin="15dp"
        android:layout_alignParentBottom="true"/>-->

</RelativeLayout>